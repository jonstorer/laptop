#!/bin/sh

# Welcome to the machine script!

# Ask for sudo upfront so we don't have to get it later
sudo -v

# Keep using sudo so the permissions don't time out by updating existing sudo time stamp if set, otherwise do nothing.
while true; do sudo -n true; sleep 10; kill -0 "$$" || exit; done 2>/dev/null &

# shellcheck disable=SC2154
trap 'ret=$?; test $ret -ne 0 && printf "failed\n\n" >&2; exit $ret' EXIT
set -e

sudo apt update
sudo apt full-upgrade

sudo apt-get install -y ca-certificates curl gnupg

# Skip Docker setup when LAPTOP_SKIP_DOCKER=1 (e.g. WSL with Docker Desktop)
if [ -z "${LAPTOP_SKIP_DOCKER}" ] && [ ! -e "/etc/apt/keyrings/docker.gpg" ]; then
  sudo install -m 0755 -d /etc/apt/keyrings
  curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
  sudo chmod a+r /etc/apt/keyrings/docker.gpg

# shellcheck disable=SC1091
  echo \
    "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
    $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
    sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
fi

sudo apt full-upgrade

apt_packages="build-essential gcc libssl-dev zsh exuberant-ctags git htop hub jq gnupg2 openssl openssh-server silversearcher-ag shellcheck tmate tmux vim watch"
if [ -z "${LAPTOP_SKIP_DOCKER}" ]; then
  apt_packages="$apt_packages docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin"
fi
# shellcheck disable=SC2086
sudo apt install -y $apt_packages

# Install asdf and Node.js
if [ ! -d "$HOME/.asdf" ]; then
  echo "Setting up asdf and Node.js ..."
  git clone https://github.com/asdf-vm/asdf.git "$HOME/.asdf" --branch v0.14.1
  # shellcheck disable=SC1091
  . "$HOME/.asdf/asdf.sh"

  asdf plugin add nodejs 2>/dev/null || true
  asdf install nodejs lts 2>/dev/null || true
  node_version=$(asdf list nodejs 2>/dev/null | tail -1 | sed 's/^[ *]*//')
  if [ -n "$node_version" ]; then
    asdf global nodejs "$node_version"
    echo "Node.js $node_version set as global default"
  fi

  echo "legacy_version_file = yes" > "$HOME/.asdfrc"
  echo "Created ~/.asdfrc for .nvmrc / .node-version support"
fi

if [ -d "$HOME/.asdf" ] && ! grep -q "asdf.sh" "$HOME/.zshrc" 2>/dev/null; then
  {
    echo ''
    echo '# asdf version manager'
    # shellcheck disable=SC2016
    echo '. "$HOME/.asdf/asdf.sh"'
  } >> "$HOME/.zshrc"
  echo "Added asdf to ~/.zshrc"
fi

shell_path="$(command -v zsh)"
echo "Ensure shell is zsh at '$shell_path' ..."
if ! grep "$shell_path" /etc/shells > /dev/null 2>&1 ; then
  echo "Add '$shell_path' to /etc/shells"
  sudo sh -c "echo $shell_path >> /etc/shells"
fi
sudo chsh -s "$shell_path" "$USER"
